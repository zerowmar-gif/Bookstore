# 📚 Bookstore 

Консольний застосунок для обліку продажу книг у книжковому магазині.

---

## 🚀 Основний функціонал

### 👨‍💼 Управління співробітниками (CRUD)
- Додавання
- Перегляд списку
- Перегляд деталей
- Редагування
- Soft delete (логічне видалення)

### 📚 Управління книгами (CRUD)
- Додавання
- Перегляд
- Редагування
- Контроль залишків
- Soft delete

### 💰 Управління продажами
- Створення продажу
- Перевірка існування співробітника та книги
- Перевірка залишку на складі
- Транзакційність (продаж + зменшення кількості виконуються атомарно)

---

## 📊 Звіти

- Повна інформація про співробітників
- Повна інформація про книги
- Повна інформація про продажі
- Продажі за дату
- Продажі за період
- Продажі конкретного співробітника
- Найбільш продавана книга за період
- Найуспішніший продавець за період (за прибутком)
- Сумарний прибуток за період
- Автор, який найбільше продається 
- Жанр, що найбільше продається
- Експорт звітів у CSV (папка `export/`)

---

## 🛠 Технології

- Python 3.13
- PostgreSQL
- psycopg (v3)
- SQL (JOIN, GROUP BY, агрегатні функції)
- Регулярні вирази (валідація email)

---

## 🗄 Ініціалізація БД

При першому запуску:

- автоматично створюється база `bookstore` (якщо її не існує)
- створюються таблиці
- додаються seed-дані (4 співробітники, 10 книг, 10 продажів)

При повторному запуску дані не дублюються.

---

## ⚙️ Налаштування

У файлі `database.py` необхідно вказати свої параметри підключення:

```python
USER = "postgres"
PASSWORD = "your_password"
HOST = "localhost"
DB_NAME = "bookstore"
```

▶️ Запуск

1. Встановити PostgreSQL.


2. Встановити залежності:

```
pip install -r requirements.txt
```

3. Запустити застосунок:

```
python main.py
```

📁 Структура проєкту:

```
Bookstore/
├── main.py
├── crud.py
├── database.py
├── reports.py
├── requirements.txt
├── .gitignore
├── README.md
└── export/
```

Експорт даних:

```
Експортовані файли зберігаються в export/
```